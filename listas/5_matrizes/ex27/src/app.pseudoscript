modelo Matriz:
    construtor():
        Matriz.matriz = [1,2,3][1,2,3]
        
    funcao start():
        cada valorx em [1..3]
            cada valory em [1..3]
                entrada("Introduza um numero para a fila {valorx} columna {valory}: " Matriz.matriz[valorx][valory]);